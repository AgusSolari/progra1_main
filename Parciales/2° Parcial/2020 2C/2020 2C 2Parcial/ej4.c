//	EJERCICIO 4
// Explicar similtudes, ventajas y desventajas entre crear una variable en el stack o en el heap. Dar un ejemplo concreto en donde conviene utilizar cada caso

/*
 * El heap es un alamcenamineto dinamico el cual nos permite crear variables o reservar memoria sobre este en tiempos de ejecucion, a diferencia de reservar memoria
 * en el stack que se realiza en tiempo de compilacion.
 *
 * Entre las similitudes podriamos decir que ambos nos sirven para un mismo proposito el cual es resevrar una o varias posiciones de memoria
 * para almacenar nuestros datos, ya sean variables, punteros, arreglos de datos, entre otros.
 *
 * Una desventaja con respecto al heap es que es mas lento en tomar ciertas posiciones de memoria, ya que como se menciono esto se realiza en timepos de ejecucion,
 * y puede tomar timepo dicha busqueda de zoanas de memoria vacias, hasta incluso podria no crearse esta zona cosa que con el stack no pasaria nunca
 *
 *
 */

